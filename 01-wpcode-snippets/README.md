# WPCode Snippets

Drop the WPCode export `.json` here (Code Snippets → Tools → Export), or one
file per snippet named `NN-snippet-title.php`.

For manual exports, record each snippet's **Type**, **Location**, **Priority**,
and **Status** in a header comment — see EXTRACTION-GUIDE.md Step 1. Those
settings matter as much as the code.

Fill in below:

- Total snippets: 
- Active: 
- Inactive: 
