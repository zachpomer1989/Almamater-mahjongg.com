# Divi Code

Two things go here:

1. `theme-options-integration.txt` — the four boxes from
   **Divi → Theme Options → Integration**, each labeled.
2. Divi layout / library export `.json` files from **Portability → Export**,
   for any page containing a Code module.

Note here which pages contain Code modules:

- 
