# Page Source

Raw View Source HTML (`Ctrl+U` / `Cmd+Opt+U`) for the homepage and key pages.

Use View Source — **not** "Save Page As" and **not** DevTools' Elements
panel. We need the server's raw response before JavaScript rewrites the DOM.

Suggested files: `homepage.html`, plus 2–4 important interior pages named
after their slug.
